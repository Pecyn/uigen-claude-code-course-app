Stage and commit changes $ARGUMENTS

* stage and commit changes specified in arguments
* if no arguments apply for all changes

1. stage changes with `git add`
2. generate a commit message and ask for approval
3. commit change with `git commit` 